services:
  openclaw:
    build: .
    network_mode: host
    env_file: .env
    environment:
      - NODE_OPTIONS=--max-old-space-size=512
    volumes:
      - openclaw-data:/root/.openclaw
    restart: unless-stopped

volumes:
  openclaw-data:
